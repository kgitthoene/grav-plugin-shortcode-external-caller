name: Shortcode External Caller
slug: shortcode-external-caller
type: plugin
version: 0.1.1
description: "Use the Shortcode Core Plugin to provide the [external-caller] shortcode."
icon: code
author:
  name: Kai Thöne
  email: k.git.thoene@gmx.net
  url: https://github.com/kgitthoene
keywords: grav, plugin, twig
homepage: https://github.com/kgitthoene/grav-plugin-shortcode-external-caller
bugs: https://github.com/kgitthoene/grav-plugin-shortcode-external-caller/issues
license: BSD

dependencies:
    - { name: grav, version: '>=1.6.4' }

form:
  validation: strict
  fields:
    enabled:
      type: toggle
      label: Plugin status
      highlight: 1
      default: 0
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
